// * must know
// ? 1. var, let, const
// ? 2. template string
    // ? 2.1---> create multiline string 
    // ? 2.2---> add dynamic varaible (expression): `${}`  

// ? 3.arrow function
    // ? 3.1----> single parameter arrow function 
    // ? 3.2----> two parameter arrow function 
    // ? 3.3----> no parameter arrow function 
    // ? 3.4----> arrow function with functin body. And don't forget to return the body explicitly

// ? 4. map, filter, find
// ? 5. optional chaining
// * good to have
// ? 1. default parameter 
// ? 2. spread operator(...spread)---> [798, 634,...numbers, 34, 45] 
// ? 3. destructuring
    // ? 2.1---> object destructuring--->how to get property name as variable
        // ? const {x} = {x:5, y:7}
    // ? 2.1---> array destructuring--->
        // ? const [a,b] = [3, 45, 6, 8] 

// ? 4. forEach
// ? 5. dot notaion vs bracket notaion

// * should know the name
// ? 1. reduce()
// ? 2. inheritence
// ? 3. prototypical chain
// ? 5. class, object